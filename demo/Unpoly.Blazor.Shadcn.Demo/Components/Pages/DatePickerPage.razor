@page "/components/date-picker"

<PageTitle>Date Picker — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Date Picker</h1>
<p class="doc-lede">
    <code>&lt;input type="date"&gt;</code>, which already has a calendar, a keyboard, a locale and a mobile picker. Air Datepicker enhances it where a range or a disabled-day rule is needed; the input still posts either way.
</p>

<Example Source="DatePickerExample.razor">
    <DatePickerExample />
</Example>
