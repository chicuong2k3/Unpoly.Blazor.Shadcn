@page "/components/alert"

<PageTitle>Alert — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Alert</h1>
<p class="doc-lede">
    A message about the page, not about a control. <code>role="alert"</code> is deliberately absent: that announces immediately and interrupts, which is right for something that just happened and wrong for something that was already on the page when it loaded.
</p>

<Example Source="AlertExample.razor">
    <AlertExample />
</Example>
