@page "/components/spinner"

<PageTitle>Spinner — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Spinner</h1>
<p class="doc-lede">
    For the places nothing was clicked — a region refreshing itself, a poll, a deferred fragment. A button that started an Unpoly request already grows its own from <code>.up-active</code>.
</p>

<Example Source="SpinnerExample.razor">
    <SpinnerExample />
</Example>
