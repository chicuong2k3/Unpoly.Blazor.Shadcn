@page "/components/toggle"

<PageTitle>Toggle — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Toggle</h1>
<p class="doc-lede">
    A button that stays pressed — a real <code>&lt;input type="checkbox"&gt;</code>, so it posts with the form and survives a fragment swap, announced with <code>aria-pressed</code> because a bare checkbox that looks like a button would say the wrong thing.
</p>

<Example Source="ToggleExample.razor">
    <ToggleExample />
</Example>
