@page "/components/tooltip"

<PageTitle>Tooltip — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Tooltip</h1>
<p class="doc-lede">
    Hover <em>and</em> focus, because a tooltip only a mouse can reach is one half the readers never see. Escape closes it, which the APG asks for and most implementations forget.
</p>

<Example Source="TooltipExample.razor">
    <TooltipExample />
</Example>
