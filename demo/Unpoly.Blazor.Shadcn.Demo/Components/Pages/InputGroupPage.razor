@page "/components/input-group"

<PageTitle>Input Group — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Input Group</h1>
<p class="doc-lede">
    An icon, a button or a unit inside the field's own frame. The frame carries the focus ring rather than the input, because <code>:focus-within</code> is what makes a composite control look focused.
</p>

<Example Source="InputGroupExample.razor">
    <InputGroupExample />
</Example>
