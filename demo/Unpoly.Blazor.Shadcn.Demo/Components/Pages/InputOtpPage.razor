@page "/components/input-otp"

<PageTitle>Input OTP — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Input OTP</h1>
<p class="doc-lede">
    Six real inputs, not one hidden field with painted boxes. It posts without JavaScript, <code>autocomplete="one-time-code"</code> lets a phone offer the code it has just received, and nothing needs rehydrating after a swap.
</p>

<Example Source="InputOtpExample.razor">
    <InputOtpExample />
</Example>
