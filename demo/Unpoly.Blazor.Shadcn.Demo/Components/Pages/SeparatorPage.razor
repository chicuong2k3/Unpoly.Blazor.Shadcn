@page "/components/separator"

<PageTitle>Separator — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Separator</h1>
<p class="doc-lede">
    Decorative by default, as in Radix: a rule that only draws a line is noise to a screen reader, so it is hidden from the tree unless the caller says it separates something.
</p>

<Example Source="SeparatorExample.razor">
    <SeparatorExample />
</Example>
