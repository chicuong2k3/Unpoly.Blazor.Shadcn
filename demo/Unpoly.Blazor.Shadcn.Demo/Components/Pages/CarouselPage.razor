@page "/components/carousel"

<PageTitle>Carousel — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Carousel</h1>
<p class="doc-lede">
    CSS scroll-snap. Embla exists because Safari's implementation was unusable in 2019; it is fine now, so with scripting off this still swipes and still scrolls and only the arrows go quiet.
</p>

<p class="doc-note">
    Embla exists because Safari's scroll-snap was unusable in 2019. It is fine now, so this is CSS
    scroll-snap and the arrows are all that is left in JavaScript. With scripting off it still
    swipes and still scrolls; the arrows hide rather than sit there dead.
</p>
<Example Source="CarouselExample.razor"><CarouselExample /></Example>
