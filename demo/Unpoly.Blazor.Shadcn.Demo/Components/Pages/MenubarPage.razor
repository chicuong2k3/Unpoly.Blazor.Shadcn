@page "/components/menubar"

<PageTitle>Menubar — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Menubar</h1>
<p class="doc-lede">
    Once any menu is open, hovering another opens that one instead of doing nothing. That is what makes a menubar feel like one, and it is the only part of it that is not free.
</p>

<p class="doc-note">
    Once any menu is open, hovering another opens that one instead of doing nothing. That is what
    makes a menubar feel like a menubar, and it is the only part of it that is not free.
</p>
<Example Source="MenubarExample.razor"><MenubarExample /></Example>
