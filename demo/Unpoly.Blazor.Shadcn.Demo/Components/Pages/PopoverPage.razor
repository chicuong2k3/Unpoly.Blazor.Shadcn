@page "/components/popover"

<PageTitle>Popover — Unpoly.Blazor.Shadcn</PageTitle>

<h1 class="doc-h1">Popover</h1>
<p class="doc-lede">
    The <code>[popover]</code> API. It can be measured against something other than its trigger, which is what <code>PopoverAnchor</code> is for.
</p>

<h2 class="doc-h2">Popover</h2>

<Example Source="PopoverExample.razor">
    <PopoverExample />
</Example>

<h2 class="doc-h2">Pieces that only make sense inside another</h2>

<p class="doc-note">
    A separator that carries a word, a group with a header and a footer, and a popover measured
    against something other than its trigger.
</p>
<Example Source="CompositionExample.razor">
    <CompositionExample />
</Example>
