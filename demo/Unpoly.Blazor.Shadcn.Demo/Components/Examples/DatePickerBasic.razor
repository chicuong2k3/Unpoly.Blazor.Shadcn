@* <input type="date">. It already has a calendar, a keyboard, a locale, a mobile picker and a
   posted value — and Air Datepicker enhances it where a range or a disabled-day rule is needed.
   The input posts either way. *@
<Field Class="w-[280px]">
    <FieldLabel For="dp-basic">Starting</FieldLabel>
    <DatePicker id="dp-basic" name="starts" value="2026-03-11" />
</Field>
