@* A real input, so the label points at it with `for` and clicking the words toggles the box —
   which is a bigger target than the box and the reason a label is not decoration. *@
<Field Orientation="horizontal">
    <Checkbox id="terms" name="terms" value="yes" />
    <FieldLabel For="terms">Accept the terms</FieldLabel>
</Field>
