@* The label dims with the input because the recipe reads :disabled on it, not because the two
   were set separately and have to agree. *@
<div class="flex items-center gap-3">
    <Toggle disabled aria-label="Not available"><Icon Name="check" /></Toggle>
    <Toggle Checked disabled>Locked on</Toggle>
</div>
