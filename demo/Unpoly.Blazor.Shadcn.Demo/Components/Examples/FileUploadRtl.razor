@* Mirrored: the remove button rides on the reading-end corner, which is the left in Arabic.
   Nothing here names a side — the button is positioned with `end-2`. *@
<div dir="rtl" class="w-full max-w-sm">
    <FileUpload Name="cover-rtl" Action="/uploads" Prompt="أفلِت صورة هنا أو انقر للاختيار"
                Hint="حتى 8 ميجابايت" ClearLabel="إزالة" />
</div>
