@* The toast stack moves to the other corner, because ui.behavior.css positions it with inline
   insets rather than with right. *@
<div dir="rtl">
    <Button Variant="outline" onclick="toast('تم الحفظ')">إظهار</Button>
</div>
