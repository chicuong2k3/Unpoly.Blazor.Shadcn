@* Align="start" means the reading edge, not the left one: the panel is measured against the
   trigger, and the measurement already knows the direction. *@
<div dir="rtl">
    <PopoverTrigger Target="pop-rtl" Variant="outline">الأبعاد</PopoverTrigger>
    <PopoverContent Id="pop-rtl" Align="start" Class="w-64 p-3 text-sm">
        لوحة في الطبقة العليا.
    </PopoverContent>
</div>
