@* The spin is a rotation, so it does not mirror — but the gap between it and the label does. *@
<div dir="rtl" class="flex items-center gap-3">
    <Button disabled><Spinner />جارٍ الحفظ…</Button>
    <Badge Variant="secondary"><Spinner Class="size-3" />مزامنة</Badge>
</div>
