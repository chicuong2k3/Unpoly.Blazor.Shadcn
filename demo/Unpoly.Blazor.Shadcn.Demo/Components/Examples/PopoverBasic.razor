@* The [popover] API: the top layer, light dismiss and Escape, none of it written here. *@
<PopoverTrigger Target="pop-basic">Open</PopoverTrigger>
<PopoverContent Id="pop-basic" Class="w-72 p-4 text-sm">
    A panel in the top layer, so it escapes any overflow it is written inside.
</PopoverContent>
