@* Three thumbs, as upstream's defaultValue={[10, 20, 70]}. A thumb stops at its neighbour
   rather than crossing it, and the band runs between the outermost two. *@
<Slider Name="stops" Min="0" Max="100" Step="10" Values="new double[] { 10, 20, 70 }"
        Class="mx-auto w-full max-w-xs" />
