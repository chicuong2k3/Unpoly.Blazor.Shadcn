@* The hint on the right is decorative: the shortcut itself is bound elsewhere, and reading "⌘K"
   aloud after the label helps nobody. *@
<DropdownMenu>
    <DropdownMenuTrigger Target="dd-shortcuts">File</DropdownMenuTrigger>
    <DropdownMenuContent Id="dd-shortcuts" Class="w-56">
        <DropdownMenuItem>New tab<DropdownMenuShortcut>⌘T</DropdownMenuShortcut></DropdownMenuItem>
        <DropdownMenuItem>New window<DropdownMenuShortcut>⌘N</DropdownMenuShortcut></DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Print<DropdownMenuShortcut>⌘P</DropdownMenuShortcut></DropdownMenuItem>
    </DropdownMenuContent>
</DropdownMenu>
