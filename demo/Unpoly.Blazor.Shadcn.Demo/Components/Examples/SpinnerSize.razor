@* size-* on the element. It is an icon, so it sizes like one and inherits the colour it sits in. *@
<div class="flex items-center gap-4">
    <Spinner Class="size-3" />
    <Spinner />
    <Spinner Class="size-6" />
    <Spinner Class="size-8 text-primary" />
</div>
