@* A long message folded. <details> again: it opens with no script, survives a fragment swap, and
   Find in Page reaches inside it in most browsers — none of which a state variable manages. *@
<Bubble Variant="muted" Class="w-[440px]">
    <BubbleContent>
        <Collapsible>
            <p>The extractor reads shadcn's own source rather than a class string somebody typed.</p>
            <CollapsibleContent Class="mt-2 flex flex-col gap-2">
                <p>That is what makes the parity claim checkable: the tests compare against the
                   same vendored file the generator wrote from.</p>
                <p>When upstream changes, the diff is real source rather than a screenshot.</p>
            </CollapsibleContent>
            <CollapsibleTrigger Class="mt-2 text-sm underline underline-offset-2">
                Show more
            </CollapsibleTrigger>
        </Collapsible>
    </BubbleContent>
</Bubble>
