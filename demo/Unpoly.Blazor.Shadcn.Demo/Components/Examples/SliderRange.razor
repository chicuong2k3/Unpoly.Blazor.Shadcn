@* Two thumbs, one strip. Underneath: two native inputs stacked, each taking the pointer only
   on its own thumb, both posting under the one name — which a server reads as a list. *@
<Slider Name="range" Min="0" Max="100" Step="5" Values="new double[] { 25, 50 }"
        Class="mx-auto w-full max-w-xs" />
