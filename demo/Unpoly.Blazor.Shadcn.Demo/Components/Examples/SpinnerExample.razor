@* Worth knowing where this belongs. A button that started a request already grows its own
   spinner from Unpoly's .up-active, with no parameter and no state — so <Spinner> is for the
   places nothing was clicked: a region refreshing itself, a poll, a deferred fragment. *@
<Spinner />
<Spinner Class="size-6 text-primary" />
<Button disabled><Spinner />Saving…</Button>
