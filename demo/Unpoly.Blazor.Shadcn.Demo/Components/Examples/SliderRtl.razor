@* The track fills from the other edge and the arrow keys reverse, both from the document
   direction: an input knows which way its own value grows. *@
<div dir="rtl" class="w-[420px]">
    <Field>
        <FieldLabel For="sl-rtl">مستوى الصوت</FieldLabel>
        <Slider id="sl-rtl" name="volume" Value="60" />
    </Field>
</div>
