@using Unpoly.Blazor.Shadcn.ECharts.Components
@using Unpoly.Blazor.Shadcn.ECharts.Models
@* Funnel: sequential stages narrowing to the conversion. *@
<EFunnelChart Height="280px" Data="@data" />

@code {
    IEnumerable<EChartDataPoint> data = [new("Visit", 100), new("Inquiry", 80), new("Order", 60), new("Pay", 40)];
}
