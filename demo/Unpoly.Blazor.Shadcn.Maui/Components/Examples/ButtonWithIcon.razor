@* Leading and trailing, decided by source order alone: the button is a flex row with a gap and
   sizes any svg inside it, so an icon needs no class and no wrapper either side. *@
<div class="flex flex-wrap items-center gap-2">
    <Button Variant="outline"><Icon Name="git-branch" />New Branch</Button>
    <Button Variant="outline">Fork<Icon Name="git-fork" /></Button>
</div>
