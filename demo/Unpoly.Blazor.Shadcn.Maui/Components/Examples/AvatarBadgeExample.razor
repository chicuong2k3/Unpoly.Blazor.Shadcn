@* A dot for presence. It is decorative when the state is written beside it in words, as here —
   announcing it twice is worse than not styling it at all. *@
<div class="flex items-center gap-2">
    <Avatar>
        <AvatarFallback>ML</AvatarFallback>
        <AvatarBadge Class="bg-emerald-500" aria-hidden="true" />
    </Avatar>
    <span class="text-sm">Mai Linh — online</span>
</div>
