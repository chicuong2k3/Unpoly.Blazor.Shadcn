@* Screen readers announce the friendly label instead of the raw URL. Without it, AriaLabel
   falls back to Value — a wall of "https://" nobody wants read aloud. *@
<QrCode Value="https://lumeo.nativ.sh/" Size="140" AriaLabel="QR code for Lumeo homepage" />
