<div dir="rtl">
    <ScrollArea Class="h-40 w-[420px] rounded-lg border p-3">
        <div class="flex flex-col gap-2 text-sm">
            @foreach (var n in Enumerable.Range(1, 12))
            {
                <div>السطر @n — نص طويل بما يكفي ليستحق التمرير.</div>
            }
        </div>
    </ScrollArea>
</div>
