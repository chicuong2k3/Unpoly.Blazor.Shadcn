@* The simplest use: point at a URL and size it. The server renders an empty div carrying
   data-value; a up.compiler fills it with qrcodejs on every fragment swap, so a value that
   arrives via markup just re-renders. *@
<QrCode Value="https://lumeo.nativ.sh/" Size="160" />
