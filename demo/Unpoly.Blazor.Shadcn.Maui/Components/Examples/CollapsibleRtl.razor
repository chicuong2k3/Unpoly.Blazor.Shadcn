@* The tree indents with ms/ps rather than ml/pl, so the whole thing mirrors on its own. *@
<div dir="rtl" class="w-[380px]">
    <Collapsible>
        <CollapsibleTrigger Class="flex w-full items-center justify-between rounded-md px-3 py-2 text-sm font-medium hover:bg-accent">
            ما الذي في الصندوق؟
            <Icon Name="chevron-down" Class="size-4" />
        </CollapsibleTrigger>
        <CollapsibleContent Class="px-3 py-2 text-sm text-muted-foreground">
            حقيبة وغطاء وبطاقة تعليمات.
        </CollapsibleContent>
    </Collapsible>
</div>
