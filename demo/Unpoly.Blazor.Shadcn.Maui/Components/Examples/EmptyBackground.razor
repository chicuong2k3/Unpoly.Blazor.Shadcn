@* On its own ground rather than in a frame, for an empty state that fills a panel instead of
   sitting inside a list. *@
<Empty Class="w-full max-w-lg rounded-lg bg-muted/50">
    <EmptyHeader>
        <EmptyIcon><Icon Name="info" Class="size-5" /></EmptyIcon>
        <EmptyTitle>Nothing scheduled</EmptyTitle>
        <EmptyDescription>You have no deliveries this week.</EmptyDescription>
    </EmptyHeader>
</Empty>
