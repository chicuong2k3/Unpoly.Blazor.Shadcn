@* The placeholder, the caret and the text all start from the other edge, and none of it is
   configured: an input takes its direction from the document. *@
<div dir="rtl" class="w-[360px]">
    <Field>
        <FieldLabel For="in-rtl">الاسم الكامل</FieldLabel>
        <Input id="in-rtl" name="name" placeholder="اكتب اسمك" />
        <FieldDescription>كما يظهر على بطاقتك.</FieldDescription>
    </Field>
</div>
