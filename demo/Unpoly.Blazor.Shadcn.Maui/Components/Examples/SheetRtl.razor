@* Side="right" still means the right of the screen, not the reading edge — a drawer is a
   physical place. What mirrors is the content inside it. *@
<div dir="rtl">
    <SheetTrigger Target="sheet-rtl" Variant="outline">فتح اللوحة</SheetTrigger>
    <Sheet Id="sheet-rtl" Side="left">
        <SheetHeader>
            <SheetTitle>الإعدادات</SheetTitle>
            <SheetDescription>اضغط Escape للإغلاق.</SheetDescription>
        </SheetHeader>
    </Sheet>
</div>
