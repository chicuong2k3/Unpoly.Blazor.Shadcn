<Router AppAssembly="typeof(MauiProgram).Assembly" NotFoundPage="typeof(Pages.NotFound)">
    <Found Context="routeData">
        <RouteView RouteData="routeData" DefaultLayout="typeof(Layout.MainLayout)" />
        <FocusOnNavigate RouteData="routeData" Selector="h1" />
    </Found>
</Router>

@* <PageTitle> in every page needs a <HeadOutlet> in the tree to flush to the WebView's <head>.
   Without it the title never updates and nothing says why. *@
<HeadOutlet />
