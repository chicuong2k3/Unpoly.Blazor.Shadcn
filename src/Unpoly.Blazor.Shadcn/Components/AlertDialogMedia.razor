@inherits UiComponentBase

@* An image or icon above the title — the illustration on a destructive confirmation. *@

<div data-slot="alert-dialog-media" class="@Cn("mb-2 inline-flex size-16 items-center justify-center rounded-md bg-muted sm:group-data-[size=default]/alert-dialog-content:row-span-2 *:[svg:not([class*='size-'])]:size-8", Class)" @attributes="AdditionalAttributes">@ChildContent</div>
