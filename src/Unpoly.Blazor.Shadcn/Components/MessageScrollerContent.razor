@inherits UiComponentBase

<div data-slot="message-scroller-content" class="@Classes"
     @attributes="AdditionalAttributes">@ChildContent</div>

@code {
    const string Base =
        "flex h-max min-h-full flex-col gap-8";

    string Classes => Cn(Base, Class);
}
